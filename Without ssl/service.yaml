apiVersion: v1
kind: Service
metadata:
  name: nginx-app
  namespace: dev
spec:
  selector:
    app: nginx-app     # ✅ Properly nested under selector
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort

